
are there nested columns? alert, then attempt to unpack.
drop single-valued columns (including NaNs)

displays stats on dtypes for each column

information gain IG(X)=H(Y)−H(Y|X)
  minmizes the entropy of the labels
correlation

display most 

